<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/gameRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".GameActivity">

    <!-- ФОНОВОЕ ИЗОБРАЖЕНИЕ: centerCrop предотвращает растягивание -->
    <ImageView
        android:id="@+id/ivBackground"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:scaleType="centerCrop"
        android:src="@drawable/play_base_back09"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Вставьте это сразу после ivBackground -->
    <ImageView
        android:id="@+id/cloud1"
        android:layout_width="70dp"
        android:layout_height="50dp"
        android:layout_marginTop="5dp"
        android:alpha="1"
        android:src="@drawable/cloud_png"
        app:layout_constraintStart_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/cloud2"
        android:layout_width="70dp"
        android:layout_height="50dp"
        android:layout_marginTop="10dp"
        android:alpha="1"
        android:src="@drawable/cloud_png"
        app:layout_constraintStart_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/cloud3"
        android:layout_width="70dp"
        android:layout_height="50dp"
        android:layout_marginTop="0dp"
        android:alpha="1"
        android:src="@drawable/cloud_png"
        app:layout_constraintStart_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- 1. КАРТОЧКА СЧЕТА -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cardScore"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:layout_marginHorizontal="32dp"
        app:cardElevation="4dp"
        app:cardCornerRadius="12dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">
        <!-- Сюда можно добавить ваш Score Layout -->
    </com.google.android.material.card.MaterialCardView>

    <!-- 2. ИГРОВОЕ ПОЛЕ -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cardBoard"
        android:layout_width="330dp"
        android:layout_height="330dp"
        android:layout_marginHorizontal="16dp"
        android:layout_marginBottom="-50dp"
        app:cardBackgroundColor="@android:color/transparent"
        app:cardElevation="0dp"
        app:layout_constraintBottom_toTopOf="@id/bottomPanel"
        app:layout_constraintDimensionRatio="1:1"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/cardScore"
        app:layout_constraintVertical_bias="10"
        > <!-- Смещаем поле ближе к нижней панели -->

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <ImageView
                android:id="@+id/ivBoard"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="fitCenter"
                android:src="@drawable/play_base08" />

            <com.example.tick_tack_toe_infinity.BoardTouchView
                android:id="@+id/boardTouchView"
                android:layout_width="match_parent"
                android:layout_height="match_parent" />
        </FrameLayout>
    </androidx.cardview.widget.CardView>

    <!-- 3. НИЖНЯЯ ПАНЕЛЬ С КНОПКАМИ -->
    <LinearLayout
        android:id="@+id/bottomPanel"
        android:layout_width="wrap_content"
        android:layout_height="270dp"
        android:layout_marginHorizontal="16dp"
        android:background="@drawable/down_panel01"
        android:gravity="center"
        android:orientation="vertical"
        android:paddingHorizontal="50dp"
        android:paddingTop="20dp"
        android:paddingBottom="20dp"
        android:translationY="50dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <!-- Статус игры (чей ход) -->
        <TextView
            android:id="@+id/tvStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:text="@string/game_status_your_turn"
            android:textColor="@android:color/black"
            android:textStyle="bold"
            android:textSize="22sp"
            android:fontFamily="sans-serif-medium"
            android:translationY="-20dp" />

        <!-- Кнопка Новая игра -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnNewGame"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/btn_new_game"
            android:textColor="@android:color/black"
            android:textSize="24sp"
            android:textStyle="bold"
            android:fontFamily="sans-serif-black"
            android:textAllCaps="true"
            android:translationY="-28dp"
            app:backgroundTint="@android:color/transparent"
            app:strokeWidth="0dp"
            app:elevation="0dp"
            android:stateListAnimator="@null"
            app:cornerRadius="10dp"
            app:rippleColor="#40000000" />

        <!-- Кнопка Выход -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnExit"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="BACK"
            android:textColor="@android:color/black"
            android:textSize="24sp"
            android:textStyle="bold"
            android:fontFamily="sans-serif-black"
            android:textAllCaps="true"
            android:translationY="-28dp"
            app:backgroundTint="@android:color/transparent"
            app:strokeWidth="0dp"
            app:elevation="0dp"
            android:stateListAnimator="@null"
            app:cornerRadius="10dp"
            app:rippleColor="#40000000" />
    </LinearLayout>

    <!-- Просто картинка победы: без фона, без кликов, строго по центру -->
    <ImageView
        android:id="@+id/ivWinOverlay"
        android:layout_width="350dp"
        android:layout_height="350dp"
        android:adjustViewBounds="true"
        android:clickable="false"
        android:elevation="30dp"
        android:focusable="false"
        android:translationZ="30dp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@drawable/you_won" />


</androidx.constraintlayout.widget.ConstraintLayout>